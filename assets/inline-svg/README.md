# Exported inline SVG

This folder contains standalone copies of the inline SVG currently used by the site.

- icons/: SVG files exported from the index.html symbol sprite.
- decor/: SVG files exported from dish decoration functions in script.js.
- loader/: SVG files exported from the loader markup in index.html.

The site still uses the original inline SVG; these files are an asset export/copy.
